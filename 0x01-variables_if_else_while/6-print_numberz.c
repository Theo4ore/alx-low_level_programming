#include <stdio.h>
/**
 * main - prints all single digit numbers of base 10 from 0 using putchar
 *
 * Return : 0 as SUCCESS
*/
int main(void)
{
	int q;

	for (q = 0; q < 10; q++)
	{
		putchar(q);
	}
	putchar('\n');
return (0);
}
